//
//  EndlessGame.swift
//  Panello
//
//  Created by Andrew Roberts on 4/23/17.
//  Copyright © 2017 Andrew Roberts. All rights reserved.
//

import GLKit

class EndlessGame: Game {
/*
    private var _board: Board
    private var _highScore: Int
    public var board: Board { return _board }
    public var highScore: Int { return _highScore }
    
    override init() {
        super.init()
    }
    
    func reset() {}
    func update() {
        if (_board.state == Board.BoardState.COUNTDOWN) {
            
        }
        
        if (_state == .RUNNING) {
            if (!super._panicked && _board.panicked()) {
                super._panicked = true
            }
            if (super._panicked && !_board.panicked()) {
                super._panicked = false
            }
            if (_board.score > _highScore) {
                _highScore = _board.score
            }
        }
    }
 */
}
